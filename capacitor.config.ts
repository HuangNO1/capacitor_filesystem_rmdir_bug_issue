import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap_filesystem_bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
